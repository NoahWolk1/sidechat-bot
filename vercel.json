{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/next" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/" }
  ],
  "env": {
    "PHONE_NUMBER": "${PHONE_NUMBER}",
    "GROUP_ID": "${GROUP_ID}",
    "GEMINI_API_KEY": "${GEMINI_API_KEY}"
  }
}
